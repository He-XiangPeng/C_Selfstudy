#include <stdio.h>
/** 
 * if...else用法
 */
int main(void)
{
	int i, j;
	i = 2;
	j = 3;
	scanf("%d %d", &i, &j);
	if (i >= j)
		printf("i >= j\n");	
	else
		printf("i < j\n");
		printf("BBBB\n");
	return 0;
}